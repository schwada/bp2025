apiVersion: v1
kind: Service
metadata:
  name: frontend-zakaznik-service
  namespace: bp-app
spec:
  selector:
    app: frontend-zakaznik
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer 