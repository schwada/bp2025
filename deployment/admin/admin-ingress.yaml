apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: fe-admin-ingress
  namespace: bp-app
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
spec:
  rules:
  - host: admin.bpapp.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend-admin-service
            port:
              number: 80